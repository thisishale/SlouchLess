# Install into a fresh Python venv on the Linux machine:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements-linux.txt
#
# Tkinter is NOT a pip package - install it via your distro's package manager
# before creating the venv, e.g. on Debian/Ubuntu:
#   sudo apt install python3-tk


mediapipe==0.10.35
pyinstaller==6.21.0
